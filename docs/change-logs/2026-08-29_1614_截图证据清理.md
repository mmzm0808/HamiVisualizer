# 截图证据清理

## 范围

按照项目“截图必须可追溯、旧探针不长期堆积”的约束，重新解析 `docs/` 下全部 Markdown 链接，并以日志/简报中的文件链接和目录链接作为保护清单。

## 清理结果

- 删除 12 个未被任何 Markdown 引用的中间截图：Kagome 首轮核心审计 6 张、三角外扩/紧轮廓探针 4 张、旧间距恢复探针 2 张；
- 删除两个已清空的中间审计目录；没有触碰源码、模型、用户配置或系统临时目录；
- 保留所有已链接的最终截图目录，包括最新 Kagome 三角盘和 Nx/Ny 资源提示多缩放证据；
- 清理后剩余 `437` 个非占位截图，目录仍位于项目内 `.codex-artifacts/screenshots/`。

## 校验

- Markdown 链接总数 `639`，本地链接缺失 `0`；
- 当前全量测试 `246 passed`，本次清理不改变运行时行为。
