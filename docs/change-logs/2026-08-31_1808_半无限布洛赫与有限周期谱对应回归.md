# 半无限布洛赫与有限周期谱对应回归

## 记录信息

- 开始时间：2026-08-31 18:08:01 +08:00
- 范围：公开 `HamiltonianBuilder` 路径、半无限 x-Bloch 与有限 x 周期显式提升
- 目标：将 `H0/H1/extra` 组装为测试专用的有限 x 周期块循环矩阵，并与 `kx=2πj/NX` 的 Bloch 全谱逐项核对。

## 审计方法

新增 `test_bloch_periodic_correspondence.py`。测试 helper 仅存在于测试文件，不改变产品边界 API；覆盖 `order="cell"` 与 `order="site"`、一站点 `+1/+2` 长程复幅度、负偏移规范化和实际两站点 Haldane 斜元胞的复相位 NNN。有限周期矩阵使用显式 wrap，比较其全部厄米本征值与半无限矩阵在离散波矢上的本征值拼接，误差上限为 `1e-9`。

另以 `NX=7` 的一站点 OBC 长程模型验证不发生周期 wrap，并确认 `+1` 与 `+2` 两个方向的越界计数分别为 `1` 和 `2`，合计 `skipped.oob == 3`。

## 结果

- 定向回归：`4 passed in 0.69s`；未发现生产公式不一致或需修改的生产缺口；
- 未涉及 `a1_y != 0` 的合约判断；本轮只验证谱与现有坐标/偏移语义；
- 未修改 GUI、生产 API、模型格式或数值算法，因此不新增视觉截图。

## 复核

- 完整回归：`389 passed in 285.10s`（2026-08-31 18:17:30 +08:00）；`compileall` 和 `git diff --check` 均通过。
- 简报标题采用本次最终验证完成时间 `18:17:30`；文件名中的 `1808` 保留为审计开始时间，避免把“开始时间”和“完成时间”混作排序依据。
