# 持续打磨基线审计

- 开始时间：2026-08-28 02:18:13 +08:00
- 项目：HamiVisualizer
- 工作区：`E:/ProgramData/SEU_Data/程序设计/科研/HamiVsualizer`
- Python：`D:/ProgramData/Anaconda/miniconda3/envs/python312_TempforPDF/python.exe`

## 本轮目标

对当前代码进行持续、可追溯的功能与体验审计；每完成一项修改均建立独立日志，
并以自动测试和离屏截图双重验证。最终生成 `docs/持续打磨简报.md`。

## 基线发现

1. 豆包报告的蜂窝连线舍入问题修复已存在：显示层保存精确源坐标，目标坐标
   不再由六位小数源坐标叠加精确位移得到。
2. 深色主题已经下发到矩阵、晶格、能带、波函数和比较窗格。
3. 当前测试共收集到 102 项，但在真实用户 `~/.hvisual` 工作区存在两个模型、
   自动保存开启且主题为深色时，测试在首个主题用例处卡住。因此“102 项通过”
   目前不能作为可复现基线。
4. 深色主题复选框选中态只有蓝色实心方块，没有勾号；折叠组使用的 Unicode
   三角符号在当前字体环境中显示为方框。
5. 晶格键类型仍以 `距离 <= 1.05` 判断 NN，对缩放后的自定义晶格不泛化；
   显示拓扑仍依赖六位小数坐标哈希。
6. 斜原胞 `a1/a2` 只能从模板/文件载入，用户编辑 `Lx/Ly` 会静默切回正交元胞。
7. 大于 64 阶的矩阵无论放大多少都不会生成矩阵元文字。

## 基线视觉证据

本日志记录的两张原始审计图来自早期工作区临时目录，已不再随项目保留；后续每轮
验收均改为在项目 `.codex-artifacts/screenshots` 下重新生成并建立可追溯链接，避免
文档指向失效的历史临时文件。

## 下一步

优先隔离测试工作区，消除模态弹窗导致的测试卡死；只有得到可重复的全量测试
基线后，才继续批量修改 UI 与计算逻辑。
