# 截图证据链清理

- 时间：2026-08-29 13:53（+08:00）
- 范围：`.codex-artifacts/screenshots/`、`.codex-artifacts/test-runs/` 以及引用这些证据的 Markdown 文档。
- 性质：仅整理验证产物，不修改源码、模型、计算结果或用户配置。

## 清理动机

连续多轮离屏验收留下了大量探针截图，其中一部分后来没有被任何日志或简报引用。它们会增加项目体积，也会让审查者难以区分当前基线与已被替代的临时画面。本轮按“被文档直接引用才保留”的规则复核；若文档链接到整个证据目录，则目录下全部截图视为受保护，避免误删可追溯证据。

## 执行结果

- 清理前截图文件（不含 `.gitkeep`）：418 个；其中受文档引用保护 391 个。
- 精确删除未被引用且位于项目截图目录内的 27 个旧探针文件；未删除任何源码、模型、日志或系统用户目录文件。
- 清理后保留 390 个非占位截图，均可通过日志或持续简报追溯到对应功能验收组。
- `.codex-artifacts/test-runs/` 仅保留 `.gitkeep`，没有遗留测试运行目录。
- Markdown 链接复核（包含后续 14:18、14:38 条目及其全部代表截图链接）：620 条链接（618 条本地链接），缺失 0 条；扫描唯一提示仍是既有 `.pytest_cache` 目录权限警告。

## 安全边界

清理脚本先解析每个 Markdown 相对链接，再将目录链接展开为目录保护集合；每个待删路径均经过 `Resolve-Path`，并验证位于项目专属 `.codex-artifacts/screenshots` 根目录下。没有使用宽泛 glob，也没有触碰 `~/.hvisual`、系统临时目录或工作区其他项目。

## 对功能的影响与后续

本轮不改变运行时行为，因此无需重新计算模型；最近一次完整回归仍为 `239 passed in 156.21s`（2026-08-29 13:31，1 个既有 Windows 中文路径 `.pytest_cache` 权限警告）。后续新截图应优先复用已有证据组或使用新的时间戳目录，并在日志/简报中留下链接，避免再次产生不可追溯文件。
