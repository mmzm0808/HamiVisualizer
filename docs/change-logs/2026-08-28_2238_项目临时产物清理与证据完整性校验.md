# 项目临时产物清理与证据完整性校验

- 时间：2026-08-28 22:38 +08:00
- 范围：`.codex-artifacts` 临时截图/测试运行目录、日志链接完整性

## 清理前审计

- `.codex-artifacts` 中共发现 234 张 PNG；逐份扫描 `docs/**/*.md` 的相对链接后，只有 128 张被日志或简报引用；
- 其余 106 张属于旧诊断裁剪图、同一功能的中间截图或已被后续版本替代的重复图，共约 11.67 MB；
- 测试运行目录残留 712 个 `.hvisual/.json` 临时文件，共约 25.45 MB；这些文件没有文档引用，也不是用户模型。

## 执行的整理

- 仅删除未被任何项目文档引用的 106 张 PNG；保留所有引用证据，包括本轮预设单位跃迁、半无限胞间和侧栏新增跃迁截图；
- 删除精确路径 `.codex-artifacts/test-runs/` 下的 712 个临时测试文件；后续测试仍按项目配置在该目录生成并在运行结束时清理；
- 删除源码树中 5 个 Python `__pycache__` 目录；
- 没有删除源码、正式文档、`.hvisual` 用户模型、用户 `~/.hvisual` 工作区或无法确认归属的文件；
- `.pytest_cache` 保留，因为 Windows 中文路径下存在锁定权限，强删会扩大风险。

## 完整性验证

- 清理后剩余 PNG：128 张，全部有文档引用；
- 文档图片链接缺失：0；
- 预设单位跃迁截图组 8 张全部存在，侧栏新增跃迁截图和半无限对话框截图均存在；
- 清理前后的全量测试：最终 `189 passed in 33.16s`；清理发生在测试完成后，不改变代码行为；
- 项目外未新增 HamiVisualizer 临时文件，运行时用户配置仍仅写入既定 `~/.hvisual`。

## 影响与回滚

- 删除对象都是未引用的临时证据或测试生成物，不影响运行和文档审查；
- 如需追溯旧中间截图，应从版本控制/系统回收机制恢复；当前正式证据均已保留并通过链接校验。
