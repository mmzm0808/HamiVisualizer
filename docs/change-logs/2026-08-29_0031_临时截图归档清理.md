# 临时截图归档清理

## 清理范围

项目内截图已按“仍被 Markdown 引用的证据优先”重新盘点。删除了已被更完整的 180% 全预设审计替代、且没有任何文档引用的 `editor-cross-preset-audit-20260829` 旧截图目录（12 张），以及边界状态审计中未被引用的浅色 100% 和深色 100% 两张重复截图。保留所有简报/变更日志引用的截图、25 张全预设巡检组和最新 180% 编辑框证据。

## 测试临时文件

全量测试生成的 `.codex-artifacts/test-runs/pytest-default`（19 个临时模型、27 个 JSON）已在测试完成后清理，仅保留目录占位 `.gitkeep`。没有删除任何用户模型或 `~/.hvisual` 数据。

## 结果

- 截图从 181 张 / 23.39 MB 降至 167 张 / 21.24 MB；
- 文档相对链接检查：0 个缺失；
- 项目相关产物仍全部位于 `HamiVisualizer/.codex-artifacts`，没有新增工作区顶层或系统临时文件。

本轮仅清理冗余证据，不影响程序代码和模型语义。
