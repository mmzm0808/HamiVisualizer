# 数学排版与密集交互复核

## 复核范围

针对此前 `e^{ik_x}` 中下标位置容易被误认为落到指数基线下方的问题，重新用当前数学排版器检查半无限蜂窝矩阵；同时复核本轮密集跃迁编辑的右侧导轨在放大矩阵/不同主题下是否仍保持清晰。

## 结果

- `e^{ik_x}`、`e^{-ik_x}` 均保持整体指数上移，`x` 只作为 `k` 的下标，不再掉到主基线；
- Cambria Math / STIX Two Math / DejaVu Serif 回退链正常，深色主题中数学文字与色块对比足够；
- 右侧系数编辑器保持固定像素尺寸，密集模式初始没有交叉辅助引线；
- 矩阵行列标尺、参数卡片和输入框边框在当前 100% 视图均未出现裁切。

## 离屏证据

- [半无限蜂窝矩阵·浅色 100%](../../.codex-artifacts/screenshots/math-current-audit-20260830/honeycomb-matrix-light-100.png)
- [半无限蜂窝矩阵·深色 100%](../../.codex-artifacts/screenshots/math-current-audit-20260830/honeycomb-matrix-dark-100.png)

## 结论

当前数学排版和密集编辑导轨的组合路径未发现新的裁切或基线回归；复杂的物理 NNN 线仍属于真实模型连线，不应与编辑辅助引线混淆。
