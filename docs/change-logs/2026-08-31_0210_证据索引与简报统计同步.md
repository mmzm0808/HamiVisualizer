# 证据索引与简报统计同步

- 时间：2026-08-31 02:10:35 +08:00（北京时间）
- 范围：项目内持续打磨简报与离屏验收证据索引
- 类型：文档一致性与回归防护，不改变软件运行逻辑

## 发现的问题

当前项目的 `.codex-artifacts/` 目录新增了两张 100% 数学排版复核图，但 `docs/持续打磨简报.md` 的摘要仍保留上一轮的 `676` 张统计。统计过期不会影响程序启动，却会让代码审查和发布前检查得到错误的证据数量。

## 处理方式

1. 递归统计 `.codex-artifacts/` 下的 `.png/.jpg/.jpeg/.webp` 文件，确认当前数量为 `678`。
2. 只更新简报摘要的当前统计为 `678`；历史条目中记录的 `663 → 674` 保持不变，避免篡改历史审计事实。
3. 在 `test_ui_hardening.py` 增加一致性回归：本地证据目录存在时，自动将文件数与摘要中的数字比较；在源码分发不含证据目录的干净环境中安全跳过。

## 证据

- 浅色数学排版（100%）：`../.codex-artifacts/screenshots/current-math-audit-100-20260831/honeycomb-matrix-light-100.png`
- 深色数学排版（100%）：`../.codex-artifacts/screenshots/current-math-audit-100-20260831/honeycomb-matrix-dark-100.png`

## 验证标准

- 简报 Codex 标题继续按北京时间从新到旧排列；
- 当前证据目录文件数与摘要一致；
- 新增审计不依赖 Qt 窗口，不改变矩阵、能带、波函数、晶格编辑或模型文件格式；
- 既有全量测试无新增回归。
