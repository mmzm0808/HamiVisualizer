# 截图证据精简与当前引线验收

## 时间与范围

- 时间：2026-08-29 23:04 +08:00
- 范围：`.codex-artifacts/screenshots/` 内的离屏验收证据；不修改模型文件、源代码和用户配置。
- 触发原因：连续迭代后截图目录累计了大量同一功能的中间稿，既增加项目体积，也让后续检查难以找到当前结果。

## 清理策略

1. 扫描 `docs/**/*.md` 中的本地 PNG 链接，逐个解析为绝对路径；日志明确引用的证据全部保留。
2. 保留本轮最新的三组证据目录：
   - `leader-declutter-20260829`：默认、单条引线和最终导轨状态；
   - `leader-decouple-20260830`：80%/100%/150%/180% 浅色/深色多倍率；
   - `leader-decouple-all-20260830`：10 个默认预设的当前全系数导轨状态。
3. 删除其余未被日志引用、且不属于上述最新证据组的中间 PNG；删除清理后留下的空目录。
4. 删除前对每个路径检查其位于项目截图根目录内，避免误删工作区外文件。

## 结果

- 截图数量：`980 → 304`，删除 676 张已被替代的中间截图；304 张均可由日志链接或当前证据组追溯。
- 项目外未写入临时文件；所有证据仍位于 `HamiVisualizer/.codex-artifacts/screenshots/`。
- Markdown 链接检查：`785 条，本地缺失 0`。
- 最新全预设证据仍完整保留：NP、SC、空白自定义、方格、一维链、蜂窝、三角、Kagome、SSH、Haldane，各有浅/深主题截图。

## 当前引线验收结论

- “系数：全部显示”只显示右侧纵向系数栏，默认不绘制全量辅助引线；
- 悬停或聚焦单个跃迁时只显示该行的斜向＋横向虚线，离开后自动收起；
- “显示次近邻”单独控制真实 NNN/长程物理层，未再与系数栏绑定；
- 代表截图：[NP 深色](../../.codex-artifacts/screenshots/leader-decouple-all-20260830/NP-dark-100.png)、[Haldane 深色](../../.codex-artifacts/screenshots/leader-decouple-all-20260830/Haldane-dark-100.png)、[Kagome 180% 深色](../../.codex-artifacts/screenshots/leader-decouple-20260830/kagome-all-180-dark-180.png)。

## 后续维护规则

- 新一轮截图优先覆盖同一证据目录或创建带日期的新目录；
- 完成验收后只在日志中保留代表图和必要的多倍率样本；
- 每次清理后必须重新运行本地 Markdown 链接检查，确认没有孤立引用。
