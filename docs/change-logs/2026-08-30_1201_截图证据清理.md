# 截图证据清理

- 日期：2026-08-30 12:01:31 +08:00
- 范围：项目专属 `.codex-artifacts/screenshots/` 目录中的重复验收截图

## 清理原则

用户允许删除过时截图。为避免误删，本次只处理同时满足以下条件的文件：

1. 位于项目自己的 `.codex-artifacts/screenshots/` 目录；
2. 没有被项目 Markdown 中的相对链接引用；
3. 同一功能日志仍保留其它代表截图或完整证据组；
4. 删除前逐个解析并确认绝对路径，没有使用工作区根目录或系统临时目录作为目标。

## 删除内容

- `close-lifecycle-audit-20260830/` 中 2 张重复窗口图；
- `parameter-value-audit-20260830/` 中 4 张重复窗口图；
- `preset-switch-audit-20260830/` 中 2 张重复窗口图；
- 合计删除 8 张，均为已有代表图之外的未引用副本；最新的 88 张全预设编辑态审计图和所有 Markdown 已引用证据均保留。

## 验证

- 清理前 PNG 数量：573；清理后：565；
- 未删除源码、模型、用户 `~/.hvisual` 数据、README 稳定截图或系统用户目录文件；
- 删除后项目专属截图目录仍可继续用于新一轮验收；后续新增截图应在日志中提供明确链接或目录索引；
- 本轮不改变运行时行为，因此无需重新计算模型。

