# 全默认预设含 SSH 实拍复核

时间：2026-08-29 20:05

## 复核范围

在当前代码版本下，统一以双开边界（OBC）、4×4 元胞、最近邻连接、1440×920 窗口和 100% 界面倍率实拍全部 9 个内置预设：NP、SC、空白自定义、方格、一维链、蜂窝、三角、Kagome、SSH；每个预设分别生成浅色和深色主题截图，共 18 张。

## 复核重点

- 参数区的滑块、输入框与内存预估不裁切；
- 矩阵的行列标尺与色块区域完整，空白自定义仍保持可用的空矩阵语义；
- 晶格格点编号、元胞轮廓和最近邻连线完整，没有因模板切换或一维退化坐标丢失；
- SSH 的 `t1/t2` 参数明确分列，双开链条显示交替跃迁结构；
- 浅色/深色主题均保持同一布局，侧栏标题、底部状态栏和选项勾选状态可读。

## 截图证据

- [18 张全预设实拍组](../../.codex-artifacts/screenshots/leader-decouple-all-20260830/)
- [SSH 端点态多倍率组](../../.codex-artifacts/screenshots/ssh-topological-audit-20260829/)

本轮未发现控件裁切、模板崩溃、矩阵维度异常或连线缺失。Kagome 正三角纳米盘仍由独立三角盘证据组验证，不将矩形 Kagome 截图冒充三角盘。

## 代码一致性

- 默认跃迁强度回归扩展到 SSH，验证 `t1=t2=1` 的默认单位尺度；
- SSH 最近邻配位数回归为每个格点 2，显式覆盖胞内键和 `off_x=1` 胞间键；
- 清理 `templates.py` 中 SSH 参数的重复初始化，避免遗留无效 `t` 参数；
- 定向回归 3 项通过；全量回归结果记录在持续打磨简报的最新条目中。
