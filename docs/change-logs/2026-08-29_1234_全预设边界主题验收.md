# 全预设边界与主题离屏验收

- 时间：2026-08-29 12:34（+08:00）
- 范围：8 个内置预设、半无限/双开两种边界、浅色/深色主题、矩阵+晶格组合页。
- 目的：补足此前主要集中在 Kagome 和 150% 的验证，确认默认模型切换后仍能保持可计算、可读、可交互，避免“某一个模型修好了、另一个模型又坏了”。

## 验收矩阵

本轮按以下笛卡尔积生成截图：

| 维度 | 覆盖 |
| --- | --- |
| 预设 | NP、SC、空白自定义、方格、一维链、蜂窝、三角、Kagome |
| 边界 | 半无限（x-Bloch）、双开（OBC） |
| 主题 | 浅色、深色 |
| 界面缩放 | 100%（基础基线；编辑器专项仍覆盖 80%/100%/150%/180%） |
| 视图 | 矩阵+晶格组合页 |

共生成 28 张截图（8×2×2，文件名中的中文模板保持原名）。证据目录：
[全部预设边界主题审计组](../../.codex-artifacts/screenshots/leader-decouple-all-20260830/)。

## 检查结果

- 8 个预设均能在两种边界下完成构建；矩阵维度与晶格格点数量一致，没有空白结果或异常错误标签。
- 半无限模型均保留 Bloch 矩阵相位和左右周期虚影；双开模型的行列标签、有限边界轮廓与实空间格点同步。
- 蜂窝斜元胞、三角模型和 Kagome 的斜/三角连线在组合视图中没有出现节点消失、连线断裂或错误的矩形外轮廓。
- 浅色/深色主题的控件、矩阵冻结行列标尺、状态栏和画布边界均在截图内完整可见；未发现输入框、按钮或文字越界。
- 代表画面已人工查看：半无限蜂窝浅色、半无限 Kagome 深色、双开蜂窝深色、双开方格浅色。其余模板截图均已保存，便于后续逐张复查。

## 自动化验证

- `test_app.py` + `test_ribbon.py`：`41 passed, 1 warning`，耗时 18.26 秒。
- `test_lattice.py`：`11 passed, 1 warning`，耗时 0.23 秒。
- 两个 warning 均为 Windows 中文路径下 pytest 无法创建 `.pytest_cache` 的既有权限提示，不影响测试结果。
- 本轮没有修改模型语义或对外接口；验证后清理了本轮临时测试运行目录，`test-runs` 仅保留 `.gitkeep`。

## 结论与后续

本轮默认预设的边界/主题基线检查通过，没有发现需要冒险改动的通用计算 bug。后续如果继续增强，应优先使用本目录的 28 张基线图做差分，尤其关注半无限虚影、Kagome 三角盘和深色主题下的编辑控件。
