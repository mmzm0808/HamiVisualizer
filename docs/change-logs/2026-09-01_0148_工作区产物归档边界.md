# 工作区产物归档边界

## 记录时间

- 完成时间：2026-09-01 01:48:02 +08:00（北京时间）
- 范围：HamiVisualizer 诊断脚本与上级工作区临时目录

## 检查结果

在工作区根目录发现一个仅用于 HamiVisualizer Qt 编辑器复现的脚本
`capture_hop_editor_repro_100.py`。它不属于其他项目，也不应继续留在
`程序设计/.codex-artifacts/` 下；根目录的该临时目录已确认没有其他文件。

## 处理

- 将脚本归档到项目专属目录
  `HamiVisualizer/tools/diagnostics/capture_hop_editor_repro_100.py`，保留其
  100% 离屏复现能力；移动后脚本的项目根路径解析仍正确。
- 删除已确认为空的上级 `程序设计/.codex-artifacts/` 临时目录；没有触碰
  `科研/HamiVisualizer/.codex-artifacts/`、其他项目文件、用户模型或
  `~/.hvisual`。
- 项目现有截图、测试运行文件和诊断脚本均位于 HamiVisualizer 内部，
  上级工作区不再承载 HamiVisualizer 产物。

## 验证

- 移动前后路径存在性检查通过：旧路径不存在，新路径存在。
- 对项目 `hamivisualizer` 与 `tools` 执行 `compileall` 通过。
- 项目截图数量保持 `719`，没有因清理误删任何被简报引用的证据。
- 本轮仅整理文件边界，不改变运行时逻辑和模型格式，因此不新增截图。
