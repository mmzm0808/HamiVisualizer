# 方向相位模式兼容收口

## 记录信息

- 开始时间：2026-08-31 19:33:23 +08:00
- 完成时间：2026-08-31 19:50:24 +08:00
- 范围：`phase_mode=directional` 的模型、面板、控制器与用户文档入口
- 原则：不实现未定义的方向相位物理语义；保留 version=1 旧文档的可读、可修复和原样 round-trip 能力。

## 收口内容

- `model.hopping` 新增 `KNOWN_PHASE_MODES` 与 `SUPPORTED_PHASE_MODES` 单一事实源；`directional` 仍可构造但 numeric/symbolic `evaluate()` 均明确拒绝；
- 持久化层继续接受并原样保留 `directional`，未知 mode 仍拒绝；
- 面板新建跃迁继续只生成 `none`/`phase`；旧值在表格中保留原 token，并通过 tooltip 标注“预留·不可计算”，重建错误明确要求改为 `none` 或 `phase`；
- 控制器保留二次防线，拒绝 directional 时使结果保持 stale，不复用旧计算结果；改为 `phase` 后可正常重建；
- README 增加兼容与修复步骤。

## 验证

- 定向测试：`5 passed in 1.34s`；完整回归：`399 passed in 151.46s`（2026-08-31 19:50:24 +08:00）；
- UI 证据：`.codex-artifacts/screenshots/directional-phase-mode-100-20260831/` 下浅色错误态与深色修复态各 1 张，均为 1920×1200、100%；已使用项目 `_configure_font` 字体初始化重新生成，人工检查中文 glyph 正常、无横滚和画布遮挡；
- 当前截图 PNG 总数：`705`；本轮新增 2 张主证据及渲染器演示输出文件；
- `compileall` 与 `git diff --check` 通过。
