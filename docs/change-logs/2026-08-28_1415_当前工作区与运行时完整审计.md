# 当前工作区与运行时完整审计

- 日期：2026-08-28 14:15 +08:00
- 范围：项目文件归档、启动环境、模型工作区、矩阵/晶格组合视图、主题与高缩放状态
- 类型：只读审计与视觉回归；本轮未修改业务代码

## 审计目标

确认 HamiVisualizer 相关文件没有散落在 `程序设计` 根目录，同时用当前代码重新验证启动兼容性、模型视图、浅色/深色主题和 150% UI 缩放，不把旧截图或旧测试结果当作唯一依据。

## 文件归档结果

1. `E:/ProgramData/SEU_Data/程序设计` 根目录没有散落的 HamiVisualizer 截图、临时日志、诊断脚本或测试输出。
2. 项目源码集中在 `hamivisualizer/`，辅助脚本集中在 `tools/`，变更记录集中在 `docs/change-logs/`。
3. 截图、诊断数据和测试运行目录集中在 `.codex-artifacts/` 下。
4. `hamivisualizer/` 下的 `__pycache__` 已清理；项目根部原有且被系统锁定的 `.pytest_cache` 保留，未强制删除。
5. 模型运行数据仍按既定设计写入用户的 `~/.hvisual`，该目录只承载运行时模型/偏好，不承载本轮临时文件。

## 运行时验证

- 当前解释器：Python 3.12 环境；NumPy 1.26.4；PySide6 6.6.3。
- `_check_runtime_compatibility()` 正常通过，不再出现 NumPy 2.x ABI 启动阻断。
- 全量回归：`123 passed in 18.94s`。
- 离屏视觉回归重新生成三张当前截图：浅色 100%、深色 100%、深色 150%。

## 人工检查结论

- 矩阵与晶格组合页仍保持真实水平分屏；两侧均可见，主题切换后配色一致。
- 矩阵四边冻结行列号仍在视口边缘显示；矩阵单元格文字与视图缩放同步。
- Bloch 标签显示为 `k` 的 `x` 下标，未回退为指数同级字符。
- 蜂窝模型中心元胞为两格点斜元胞，红色最近邻与绿色次近邻层次清晰，虚影不挤入中心编辑层。
- 150% UI 缩放下参数名、长标题、矩阵、晶格和状态栏均未出现横向溢出；左侧面板可滚动承载较长表格。

## 截图证据

- [当前审计 · 浅色 100%](../../.codex-artifacts/screenshots/audit-20260828-current-light-100.png)
- [当前审计 · 深色 100%](../../.codex-artifacts/screenshots/audit-20260828-current-dark-100.png)
- [当前审计 · 深色 150%](../../.codex-artifacts/screenshots/audit-20260828-current-dark-150.png)

## 结论

本轮未发现需要立即修复的文件组织或运行时回归问题。后续新增的截图、诊断文件和测试结果继续写入项目内 `.codex-artifacts/`，不再写入工作区根目录。
