# 斜元胞半无限布洛赫契约审计

## 记录信息

- 审计时间：2026-08-31 18:28:14 +08:00
- 范围：`a1=(1, 0.5), a2=(0, 1)` 的公开 `Lattice`、`HamiltonianBuilder` 半无限与双开路径
- 结论：现有斜元胞契约在核心计算路径上自洽；未发现需要修改生产源码的不一致。

## 验证内容

新增 `test_oblique_bloch_contract.py`，使用两站点斜元胞和胞内、胞间复跃迁，覆盖：

- `order="cell"`/`"site"` 的半无限基底均为 `NY=3` 个 `a2` 层，且各层来源仅为 `cy=0,1,2`；
- 多个 `kx` 下 `to_semi()` 保持厄米；两种 basis order 的半无限本征值一致；
- `NX=3, NY=2` 的 OBC 矩阵保持厄米，坐标由 `Lattice.position()` 使用完整 `a1` 计算，x 相邻胞的 y 坐标差为 `0.5`；
- `Lattice` 元数据和位置 API 保留斜向量，不将 `a1_y != 0` 误判为错误。

ghost/画布显示侧没有适合此纯 core 审计的公开元数据接口，本轮未测 UI ghost 平移；不因此修改源码或新增截图。

## 结果

- 定向回归：`5 passed in 1.51s`；
- 生产公式未暴露不一致；未改生产源码、GUI、模型格式或边界 API；
- 完整回归：`394 passed in 185.29s`（2026-08-31 19:22:50 +08:00）；
- `compileall -q hamivisualizer tools` 通过，`git diff --check` 通过；
- 简报标题采用最终验证完成时间 `19:22:50`；文件名中的 `1828` 保留为审计开始时间；本轮没有 GUI 生产改动，因此不新增截图。
